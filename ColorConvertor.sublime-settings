{
    // Enable commas in rgb(), hsl(), etc.
    // commas are not necessary in CSS, but if you want them, set this to true
    "commas": true,

    // Should hexadecimal colors be in uppercase, or lowercase?
    "hex_case": "lower",
    // Should hexadecimal colors be in short format, e.g. #333?
    "hex_short": true,
}
