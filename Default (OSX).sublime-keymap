[
  // Copy this to your keybindings to enable the shortcut.
  // These format values are supported:
  // name, hex, hexa, color, colora, rgb, rgba, hsl, hsla, lab, laba
  // { "keys": ["ctrl+super+c"], "command": "color_convert","args": {"value": "hex"} }
]
